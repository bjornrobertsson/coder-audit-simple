# coder-audit-simple
Simple API Usage to tabulate some columns from the Audit logs

The usage of CLI and Environment may differ. You may need to consider:
* CODER_TOKEN could be CODER_SESSION_TOKEN
* The URL can be hard-coded or passed into the script
